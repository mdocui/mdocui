{
  "name": "mdocui",
  "private": true,
  "description": "Generative UI library using Markdoc syntax for LLM streaming output",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "biome check packages/",
    "lint:fix": "biome check --write packages/",
    "clean": "turbo clean",
    "playground": "tsx playground.ts",
    "release": "turbo build --force && pnpm --filter @mdocui/core publish --access public --no-git-checks && pnpm --filter @mdocui/react publish --access public --no-git-checks && pnpm --filter @mdocui/cli publish --access public --no-git-checks"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "tsx": "^4.23.13",
    "turbo": "^2.10.12",
    "typescript": "catalog:",
    "zod": "catalog:"
  },
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.13"
  }
}
